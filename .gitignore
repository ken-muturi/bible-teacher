.DS_Store
guides/**/*.pdf

# Personal Claude Code overrides (local hooks, etc.) — never shared
.claude/settings.local.json

# Large generated artifacts — kept local, not committed
guides/audio/
models/

# Local helper scripts stay out of the repo, EXCEPT the ongoing build tooling.
# (The one-time migrate_* scripts were used once to fold CSS into base.css
#  and are kept local only.)
scripts/*
!scripts/build_guides.py
