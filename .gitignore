.DS_Store
guides/**/*.pdf

# Personal Claude Code overrides (local hooks, etc.) — never shared
.claude/settings.local.json

# Large generated artifacts — kept local, not committed
guides/audio/
models/

# Local helper scripts stay out of the repo, EXCEPT the guide build tooling
scripts/*
!scripts/build_guides.py
!scripts/migrate_deepstudy.py
!scripts/migrate_categories.py
!scripts/migrate_panels.py
